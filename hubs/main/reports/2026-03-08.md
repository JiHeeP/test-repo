# 2026-03-08

- Supabase env 반영 후 plna dev 서버(3030) 기동 확인.
- `/api/conversations`로 야간 로그 저장 테스트 수행, `autoOpsApplied: true` 확인.
- `/api/todos?date=2026-03-08` 조회로 same-day todo 자동 생성 확인.
- 후속 수정: Q5 파싱 시 `첫 30분:` 접두어 중복 생성 버그 수정.
- 결과: 밤 저장 즉시 할 일 반영 흐름이 실제 데이터 기준으로 동작함.
- 정책 변경: night-log 파싱 결과는 기본적으로 `ops_backlog_items`에 저장하고, `autoApplyToToday=true`일 때만 today todo로 반영하도록 전환.
- 추가 구현: `POST /api/ops/backlog/promote` API를 추가해 backlog 항목을 사용자가 선택해 today todo로 승격 가능하게 함.
- 정책 재수정(사용자 요청): night-log 저장 시 `daily_journals`는 자동 기록하지 않고, `daily_todos`는 자동 반영(기본 `next-day`)하도록 변경. 새벽 입력(00~04시)은 전날 로그로 간주해 오늘 할일에 반영.
- 버그 수정: 보완질문 파싱 시 `1)` 응답이 `4)`, `5)`까지 붙는 문제를 해결(다음 문항 번호가 무엇이든 경계로 인식). 잘못 생성된 합쳐진 todo 1건 수동 정리.
- 품질 개선: auto todo 생성 시 텍스트 정규화/중복 통합 로직 추가(수업준비 마감 문구 표준화, `첫 30분` 중복 접두어 제거, 유사 항목 병합).
- UX 개선: `오늘의 할 일`, `이번 주 목표`, `이번 달 목표`에서 인라인 텍스트 수정(더블클릭) + 드래그앤드롭 우선순위 재정렬 + `#순번` 표시 지원.
- UX 재조정(사용자 요청): 각 카드 우측 연필 버튼으로 수정 모드 진입/완료(체크 아이콘), 수정 모드에서만 텍스트 편집/순위 드래그/삭제 가능하도록 통일.
