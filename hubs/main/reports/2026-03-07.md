# 2026-03-07

- `external/plna` ops intake API를 대화 원문(`chatText`) 파싱 모드까지 확장.
- 22:00 로그 + 22:05 보완질문(1~5) 텍스트에서 `completed/incomplete/incompleteReason/tomorrowTop/risks/deadlines` 자동 추출하도록 구현.
- 추출 결과를 기존 로직대로 `daily_journals` upsert 및 다음날 `daily_todos` insert에 연결.
- `ops/API_USAGE.md`에 chat-to-intake 사용 예시와 매핑 규칙 문서화.
- 의도: 디스코드 대화 내용을 다음날 "오늘의 할 일"로 자동 반영 가능하게 함.
- 추가 보완: `autoFromLatestConversation=true` 옵션으로 같은 날짜 `conversations` 최신 기록(source_text 우선) 자동 파싱 지원 + 다음날 todo 중복 삽입 방지.
- 자동 트리거 보강: `POST /api/conversations` 저장 시 `source_text`에 야간 로그 패턴이 있으면 `autoOps`(기본 true)로 evening intake를 즉시 반영하도록 연결.
